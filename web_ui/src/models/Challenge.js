// Might be helpful to do things related to challenges

function createChallenge(id, description)
{
    return {
        id: id,
        description: description
    };
}

function loadFromDatabase(uuid, challenge_number)
{
    // load info from the challenge number, user's uuid
}